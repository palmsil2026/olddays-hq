# CLAUDE.md — Old Days ☕ (ร้านกาแฟ) — repo ของบริษัทนี้โดยเฉพาะ

> ย้ายออกจาก `palm-hq/olddays/` เมื่อ 2026-08-16 (เก็บประวัติ git ครบ 21 commits)
> หลักการ: **ร้านกาแฟเป็นบริษัทหนึ่ง เท่ากับโรงน้ำ (`origin-hq`)** — มี repo/GAS/ชีต ของตัวเอง
> ส่วน `palm-hq` = ตัวเจ้าของ + คุณเลขา + ห้องผู้บริหาร ซึ่ง **อ่านข้อมูลของร้านแบบ read-only** ผ่านชีต
> ไม่มีโค้ดร้านอยู่ในนั้นอีก

## ระบบนี้คืออะไร
ระบบหลังบ้านร้านกาแฟ Old Days: ปิดยอดรายวัน + ค่าคอมเครื่องดื่ม + สต๊อกวัตถุดิบ + เบิกซื้อ + เมนู
- `index.html` — LIFF app (พนักงาน/ผู้บริหาร/เจ้าของใช้)
- `gas/Code.gs` — Backend บน Google Apps Script ผูกกับ Google Sheets ของร้าน
- อ่านรายละเอียดการใช้งาน/สิทธิ์/ค่าคอมใน `README.md`

## จุดเชื่อมกับระบบอื่น (ทั้งหมด "อ่านอย่างเดียว" จากฝั่งนอก)
| ใคร | อ่านอะไรของร้าน | ผ่านอะไร |
|---|---|---|
| คุณเลขา (`palm-hq/secretary`) | สรุปยอดรายวัน (แท็บ `DailyClose`) ตอบใน LINE | Script Property `OLDDAYS_SHEET_ID` ฝั่งเลขา |
| ห้องผู้บริหาร (`palm-hq/exec`) | (อนาคต) KPI ร้าน | เหมือนกัน |
| กลุ่ม LINE Old Days | รับการ์ดสรุปยอดจาก GAS ร้าน | GAS ร้านยิง LINE push เอง (ตอนนี้ยังใช้ token ของบอทเลขา — ดู "งานค้าง") |

**กติกา:** โค้ดใน repo นี้ห้าม import/พึ่ง `secretary/Code.gs` และห้ามเขียนชีตของเลขา/โรงน้ำ
ถ้าเลขาต้องรู้อะไรเพิ่ม → เพิ่มแท็บ/คอลัมน์ในชีตร้านให้เลขาอ่าน ไม่ใช่ย้ายโค้ดไปไว้ฝั่งเลขา

## งานค้างหลังย้าย
- [ ] LINE Developers → LIFF `2011084984-YNPjVDiA` → เปลี่ยน Endpoint URL เป็น Pages ของ repo นี้
- [ ] (ไม่บังคับ) ให้ GAS ร้านมี LINE token/OA ของตัวเอง แทนการยืม token บอทเลขา
- [ ] `palm-hq/board/index.html` ปุ่ม ☕ ชี้มาที่ Pages ใหม่ (patch เตรียมไว้ใน palm-hq แล้ว)

## 🔄 กติกาทำงานร่วมกันของทุกแชท (สำคัญ — กันงานชนกัน)
1. **ก่อนเริ่มงานทุกครั้ง: `git pull` ก่อนเสมอ** — มีหลายแชทแก้ repo เดียวกัน
2. **ทำเสร็จ: commit + push ทันที** ห้ามดองงานไว้ในเครื่อง/ใน scratchpad ข้ามวัน
3. **แก้อะไรที่กระทบของจริง (GAS/หน้าเว็บ) → อัปเดต `STATUS.md`** (เขียนทับสถานะ ไม่ append)
   ไฟล์นั้นบอกว่า "repo กับของที่ deploy จริง ตรงกันรึยัง" — อ่านก่อนทำงานด้วย จะได้รู้ว่าอะไรค้าง deploy
4. ประวัติการแก้ = `git log` อย่างเดียว ไม่ต้องจด changelog ซ้ำในไฟล์ .md
5. แก้ `index.html` แล้ว ต้อง copy ไปทับใน repo `OldDays-app` (Pages) ด้วยเสมอ ไม่งั้นผู้ใช้ไม่เห็น

## ตอบเป็นภาษาไทยเสมอ · ระบุทุกครั้งว่ากำลังพูดถึง "ร้านกาแฟ" (ไม่ปนกับโรงน้ำ)
